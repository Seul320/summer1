피보나치 수열은 이전 두 수를 더하여 다음 수를 만드는 규칙을 가지고 있다. 
초기 값으로 1, 1을 설정하고, 세 번째 수부터는 이전 두 수를 더하여 구한다.
이를 반복하면서 N번째 피보나치 수를 구할 수 있다.

1. 입력 받은 수 N을 저장한다.
2. 만약 N이 1 또는 2이면, 결과로 1을 출력하고 종료한다.
3. 피보나치 수 배열 fib를 초기화한다. (fib[0] = 1, fib[1] = 1)
4. 반복문을 사용하여 3부터 N까지 다음을 수행한다.
   4.1. fib[i] = fib[i-1] + fib[i-2]
5. 결과로 fib[N-1]을 출력한다.

#include <stdio.h>

int main() {
    int N;
    scanf("%d", &N);

    if (N == 1 || N == 2) {
        printf("1\n");
        return 0;
    }

    int fib[100] = {1, 1};
    for (int i = 2; i < N; i++) {
        fib[i] = fib[i - 1] + fib[i - 2];
    }

    printf("%d\n", fib[N - 1]);

    return 0;
}
